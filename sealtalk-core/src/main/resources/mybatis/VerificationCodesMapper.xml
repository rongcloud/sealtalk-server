<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.rcloud.server.sealtalk.dao.VerificationCodesMapper">
  <resultMap id="BaseResultMap" type="com.rcloud.server.sealtalk.entity.VerificationCodes">
    <!--
      WARNING - @mbg.generated
    -->
    <id column="id" jdbcType="INTEGER" property="id" />
    <id column="region" jdbcType="VARCHAR" property="region" />
    <id column="phone" jdbcType="VARCHAR" property="phone" />
    <result column="sessionId" jdbcType="VARCHAR" property="sessionId" />
    <result column="token" property="token" />
    <result column="createdAt" property="createdAt" />
    <result column="updatedAt" property="updatedAt" />
  </resultMap>

  <select id="selectByRegionAndPhone" resultMap="BaseResultMap">
    select * from verification_codes where region = #{region} and phone = #{phone}
  </select>

  <insert id="saveOrUpdate" parameterType="com.rcloud.server.sealtalk.entity.VerificationCodes">
    INSERT INTO verification_codes (region, phone, sessionId, token, createdAt, updatedAt) VALUES(#{region}, #{phone}, #{sessionId}, #{token}, #{createdAt}, #{updatedAt}) ON DUPLICATE KEY UPDATE sessionId = VALUES(sessionId), updatedAt = VALUES(updatedAt)
  </insert>
</mapper>